---
// ModelOverview component for displaying the model architecture
import { getAssetPath } from "../utils/assetPath";
---

<section class="bg-white rounded-lg shadow-sm p-6 mb-6 max-w-6xl mx-auto">
	<h2 class="text-2xl font-bold text-gray-900 mb-4 text-center">
		Model Overview
	</h2>
	<div class="flex justify-center">
		<img
			src={getAssetPath("model.png")}
			alt="InternVLA-M1 Model Architecture"
			class="max-w-full h-auto rounded-lg"
		/>
	</div>
	<p class="text-gray-600 text-center mt-3 text-sm">
		InternVLA-M1 integrates spatial grounding into the
		vision–language–action training pipeline. Given a task instruction, the
		VLM planner produces latent plans through explicit spatial prompting,
		which then effectively guides the action expert to generate control
		signals.
	</p>
</section>
